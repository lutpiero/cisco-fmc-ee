# Python packages required by the cisco.fmc collection or your playbooks.
# Inspect the collection documentation on Ansible Galaxy and add exact pins as needed.
# Example packages (uncomment and pin versions if required):
# requests>=2.28
# fmc-sdk>=1.0.0

# Leave empty if the collection does not require extra pip packages.
